{
  "name": "ethara-team-task-manager",
  "version": "1.0.0",
  "description": "Full-Stack Team Task Manager Web Application",
  "main": "server/src/index.js",
  "scripts": {
    "install:all": "npm install && cd server && npm install && cd ../client && npm install",
    "build": "cd client && npm install --include=dev && npm run build && cd ../server && npm install",
    "start": "node server/src/index.js",
    "seed": "node server/src/db/seed.js",
    "start:server": "node server/src/index.js",
    "dev:server": "node --watch server/src/index.js",
    "dev:client": "cd client && npm run dev",
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
